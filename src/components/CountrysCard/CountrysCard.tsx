import React, { FC } from "react";
import { DataFetch } from "../../types";
import { View, Text, Image, Button } from "react-native";

const CountryCard: FC<DataFetch[]> = function (countryItems) {
  console.log(countryItems);
  return <Text>Holaa</Text>;
};
